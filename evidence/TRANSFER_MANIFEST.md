# Transfer Manifest — Arm <A/B/C>

**Status:** Template. The operator fills this after copying files from the CP/M
system into the participant clone and then into the base repository as
`src_urania/`, `src_melpomene/`, or `src_blank/`.

- Participant:
- CP/M drive:
- Transfer date/time:
- Source directory / file inventory:
- Destination tree:
- Transfer method/tool:
- Source file count:
- Destination file count:
- Source total bytes:
- Destination total bytes:
- Manifest or checksum command:
- Manifest/checksum result:
- Files intentionally omitted (and why):
- Operator observations:

The transferred tree is an examination copy. Do not silently edit it after
transfer; corrections belong in the analysis record.
